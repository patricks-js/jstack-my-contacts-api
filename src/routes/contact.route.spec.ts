import { describe, it } from "bun:test";

describe("Contact Routes", () => {
  it.todo("GET /contacts should return all contacts");
  it.todo("GET /contacts/:id should return a specific contact");
  it.todo("POST /contacts should create a new contact");
  it.todo("PUT /contacts/:id should update a contact");
  it.todo("DELETE /contacts/:id should delete a contact");
  it.todo("POST /contacts should return 400 for invalid data");
});
